object Darts {

    fun score(x: Number, y: Number): Int {
        val xDouble = x.toDouble()
        val yDouble = y.toDouble()

        // 1. Calculate the distance from (0,0) using sqrt(x^2 + y^2).
        // 2. Use 'when' or 'if/else' to return points based on distance:
        //    - distance > 10.0 -> 0
        //    - distance > 5.0 -> 1
        //    - distance > 1.0 -> 5
        //    - otherwise -> 10
        
        return 0 
    }
}
